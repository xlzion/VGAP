# B0 擦除归因(观察级)判读 — 2026-08-31

蓝海延长线 B 段第一格(`run_b0_erasure.py`,Qwen2.5-7B·R-Judge n=571,纯前向+hook,无干预)。
把 S1 的"末段 lens 塌掉"升级为"**谁**把可读的注入分量写没了"。结果只在本项目。

## 方法(按方案 §B0 预注册)
- 读出方向 d = normalize(Σ_t (val_t−v̄)·W_U[t]),t 遍历冻结的纯数字 token 集(0–100)。c_ℓ=⟨h_ℓ,d⟩(原始残差,可加)。
- 残差流可加分解:c_{ℓ+1}−c_ℓ = ⟨attn_ℓ,d⟩ + ⟨mlp_ℓ,d⟩(最后位置,hook 捕获)。
- 每分支类条件分离 Δsep = E[⟨·,d⟩|attack] − E[⟨·,d⟩|benign];**负值大 = 擦除者**。
- 工程点:transformers 5.x 的 `hidden_states[-1]` 是 **post-final-norm**(只破坏最后一步可加性,非 bug;分支捕获对每层含末层都对)。可加性在 0..L-2 上校验 = **0.017** ✓。

## 结果:集中型 + 注入特异 → B1 有明确靶点

- **一致性校验过**:c_auroc L0=0.500 / mid=0.713 / last=0.520,复现 S1 曲线 B(mid>last)→ 方向 d 有效。
- **擦除者集中在最后两层**:

  | 组件 | Δsep(attack−benign 对读出的推动) |
  |---|---|
  | attn L26 | **−0.522** |
  | attn L27 | −0.453 |
  | mlp L26 | −0.296 |
  | attn L22 | −0.230 |

  **κ(3)=0.61 ≥ 0.60 → 集中型**(方案判据:少数组件负责,B1 有靶点)。
- **注入特异**:晚半层擦除量 injection=**−2.088** vs URL 对照=**−0.285**(~7× 差)→ 晚层专门写没**注入**的可读分量,不动 URL 的。这是方案要的"擦除特异性"漂亮图。

## 结论与下一步
- B0 观察级判死活**通过**:擦除集中(attn L26/L27 + mlp L26)且注入特异 → **B1 因果验证有明确靶点**。
- 待做(按方案纪律):①B0 **跨 3 宿主复现**(llama/gemma:是否同类型/同相对深度组件擦除,铁律);
  ②**B1**(mean-ablation top 擦除组件在末位置→口头 AUROC 是否回升,含三控制:随机组件/top非擦除组件/无干预,≥3 宿主同向才写头条);③B2 DoLa 解码对照。
- 诚实边界:单宿主观察;c_auroc 原始投影(0.713)略低于 normed lens(0.94),形态一致即可;跨宿主前不外推。
